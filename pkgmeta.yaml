package-as: BrewSpeak

ignore: # Files and directories beginning with a dot (such as .git) are automatically ignored, as is the pgkmeta file itself.
    - README.md

manual-changelog: CHANGELOG.txt

license-output: LICENSE.txt